# 2026-05-12 UI 优化记录

## 改动内容

### Hero fixed 定位
- Hero 从 `position: relative` 改为 `position: fixed`
- 点击文章后内容直接出现在视口顶部，无需滚动

### 侧边栏导航
- 从顶部导航改为左侧固定侧边栏
- 侧边栏毛玻璃质感（75% 不透明）
- 展开/折叠分类，支持子目录层级
- 当前文章自动高亮 + 所属分类自动展开
- 字体放大 + 艺术字体 + 始终黑色
- 移动端自动变为抽屉式（<900px），汉堡按钮触发

### 文章显示
- 文件名尾随空格修复（`药物筛选全流程记录 .md`）
- 侧边栏显示完整文件名（非数字前缀不再截断）
- 去掉了渐变标题和自动补标题逻辑
- 卡片底色统一纯白

### 流星
- 3 → 7 颗
- 拖尾方向修正为右上 -35°

### 封面按钮
- 侧边栏顶部新增 🏠 封面按钮

## 设计决策记录
- 渐变标题移除：用户要求直接去掉
- 卡片不采用分色玻璃：用户要求统一颜色
- SSR 路由保留自动补 h1（无 markdown h1 时），API 不补（由前端 JS 处理）
